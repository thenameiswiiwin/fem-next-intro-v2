'use client';

import HeaderLink from './HeaderLink';

const Header = () => {
  return (
    <header>
      Logo
      <div>
        <HeaderLink />
      </div>
    </header>
  );
};

export default Header;
